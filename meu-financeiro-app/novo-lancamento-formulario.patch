From 478d25dc27220627d4c33856778a770b11af89e3 Mon Sep 17 00:00:00 2001
From: ChatGPT <chatgpt@example.com>
Date: Thu, 24 Sep 2026 01:54:51 +0000
Subject: [PATCH] Melhora formulario de lancamento

---
 src/pages/NewEntry.jsx | 5 ++++-
 1 file changed, 4 insertions(+), 1 deletion(-)

diff --git a/src/pages/NewEntry.jsx b/src/pages/NewEntry.jsx
index 7a1b4fc..b4c08e9 100644
--- a/src/pages/NewEntry.jsx
+++ b/src/pages/NewEntry.jsx
@@ -6,11 +6,13 @@ export default function NewEntry({ onSave, onCancel }) {
   const [descricao, setDescricao] = useState("");
   const [categoria, setCategoria] = useState("Alimentação");
   const [pagamento, setPagamento] = useState("Pix");
+  const [erro, setErro] = useState("");
 
   function submit(e){
     e.preventDefault();
     const v = Number(String(valor).replace(",", "."));
-    if(!v || v <= 0 || !descricao.trim()) return;
+    if(!v || v <= 0 || !descricao.trim()) { setErro("Informe valor e descrição"); return; }
+    setErro("");
     onSave({
       id: Date.now(),
       tipo,
@@ -38,6 +40,7 @@ export default function NewEntry({ onSave, onCancel }) {
         <label>Pagamento<select value={pagamento} onChange={e=>setPagamento(e.target.value)}>
           {["Pix","Cartão","Dinheiro","Débito","Transferência"].map(x=><option key={x}>{x}</option>)}
         </select></label>
+        <p className="error">{erro}</p>
         <div className="actions"><button type="button" className="ghost" onClick={onCancel}>Cancelar</button><button className="primary">Salvar</button></div>
       </form>
     </div>
-- 
2.47.3

